{
  "name": "abs-v2-builder",
  "version": "0.23.1",
  "private": true,
  "type": "module",
  "description": "A Billion Suns 2e Shipyard - a fleet builder for A Billion Suns, 2nd Edition. All three eras, twelve factions, live rules validation.",
  "scripts": {
    "test": "node --test",
    "test:watch": "node --test --watch",
    "typecheck": "tsc --noEmit && tsc --noEmit -p web/tsconfig.json",
    "thumbs": "python scripts/make-emblem-thumbs.py",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "engines": {
    "node": ">=22.6"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "typescript": "^6.0.3",
    "vite": "^8.1.2"
  },
  "dependencies": {
    "cropperjs": "^1.6.2",
    "idb-keyval": "^6.2.1"
  }
}
