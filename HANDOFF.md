# ABS-V2 Builder — Session Handoff

The **A Billion Suns 2e Shipyard**, a fleet builder for ABS 2E. Framework-free TypeScript SPA, deployed to GitHub Pages.

**Naming.** Three words, three jobs. Kept apart on purpose. (This lived in the README until it was cut back to a README; it is a contributor convention, not something a visitor needs before the install instructions.)

| Word | What it names | Where it belongs |
| --- | --- | --- |
| **Shipyard** | The product — *and*, in Hypergrowth, the game object it is named for: the stock of hulls you requisition from. | Wordmark, `<title>`, footer credit, the Hypergrowth build screen and everything about it. Always capitalised. |
| **Fleet List** | What Armageddon and Age of Unity build: units and High-Value Personnel committed before the game, to a credits limit. The rulebook's term. | The build screen's title in those eras, the "Build a Fleet List" button, print and share copy. Capitalised as the rulebook does. |
| **fleet** | The in-game force itself, and the app's section of saved ones. | "Fleets" nav and page, fleet cards, fleet names, fleet notes. Lowercase in prose, capitalised only as the section name. |

**"fleet builder"** is the *category* the product sits in, not a name for the product or for any screen inside it. In copy a reader sees it is allowed in exactly three places, lowercase, where a stranger needs a familiar noun to know what this is: the `<meta name="description">` in `web/index.html`, the `description` in `package.json`, and the line under the title of the README. In on-screen copy it is a bug — a Hypergrowth session is not building a fleet at all, it is stocking a Shipyard, and no units exist until they are requisitioned mid-game.

Code identifiers and comments are outside the rule: `builderView`, `.builder`, `view: "builder"`, `MODE_BUILDER_SHAPE` and the `abs-v2-builder` package key all use "builder" as the shape-neutral internal word for "the screen you build on", which is exactly what it has to mean when it covers both shapes.

**Live version:** v0.23.0 (deployed to gh-pages). The per-mode builder redesign (Phases A–E) is **complete**, print setup is a real paginated preview, and the tutorial is split in two: `#/learn` (era + prep) and `#/rules` (the four phases).
(Older v0.8.0 / v0.8.26 / v0.8.46 handoffs previously lived here; superseded by this file.)

**Note on `gh-pages` history:** the branch has been published both ways — as an ordinary commit on top of the previous deploy (the worktree method below) and as a fresh orphan root via the README's `git init` + force-push recipe. The orphan pushes rewrite the branch, so a local `origin/gh-pages` ref can be stale and make a current site look undeployed. Always `git fetch origin gh-pages` and compare the `assets/index-*.js` hash in the deployed `index.html` against a fresh `npm run build` before concluding anything is unpublished.

---

## Open work

- **The `chief-engineer` HVP id collision.** The Unity prints a Chief Engineer ("increase the range of their Mother's Wing effect by +ⓜ\"") and so does the generic list ("Power to Engines for 0 CMD once per Round"). Two different people, same id, same display name. A saved fleet stores only the id, so for a Unity player the two are one selection, and the two resolvers disagree about which wins: `findHvp` (`web/render.ts`) takes the faction's first, the validator (`src/validation.ts`, `allowedHvp`) lets the generic overwrite it. The Unity's personnel list therefore renders two identical-looking cards, only one of which can ever be chosen. **Not fixed**, deliberately: renaming either id silently changes what already-saved fleets are carrying, and whether a Unity player is entitled to both is a rules question — check the book (p.152 area) before touching it. `test/data.test.ts` pins this as the one known collision, so a second one fails the suite.
- **Solo mode is still open work** (user, 2026-08-06: "solo mode still on todo"). The concrete piece is the **"Modded Blip table" for solo/enemy encounters.** The user's actual direction for buildable enemy opposition, replacing an `enemyOnly`-Foundry-faction approach that was built, then explicitly reverted (`git revert`, still in history if you want the diff) once the user realised the real solo opponent is a random Blip draw, not a fleet you assemble. Not designed yet. The ask: "many such options for the enemy blips" — i.e. a moddable/selectable set of Blip-table variants, not a single fixed table. Start from the existing Blip logic (`src/data/junkspace-solo.ts`) rather than the reverted commit.
- **Emblem quality complaint, unresolved.** User said "some of the emblems seem notably lower quality than the files i gave you a long time ago" but never named which ones after being asked. Needs a follow-up to identify the specific files before anything can be fixed.
- **All-caps / wide letter-spacing restyle.** ~79 CSS rules use `text-transform: uppercase` + `letter-spacing` (the house "label" style: `.control-label`, `.sheet-section`, `.nfd-*`, `.learn-*`, print headers…). The user asked for the full list, then said to leave it for now. If revisited, decide once: drop the letter-spacing, sentence-case everything but tiny micro-labels, or keep.
- **The "General" emblem bucket.** 88 of 153 library sigils are loose files at `web/emblems/` root, so they all land in the catch-all "General" folder. The other 11 folders are tidy. Filing those into folders is a content decision for the user, not code.
- **Play Mode depth.** Research (see below) suggests the biggest wins would be: phase-filtered command list (show only commands legal in the current phase), per-ship damage/shield tracking, and an activation-order tracker. Currently Play Mode is a phase/round/CMD/VP tracker only.
- **Landscape play mode.** Previously scoped, not started.

### The three modes — the mental model (implemented)

| Mode | You build a… | Ships | HVPs (High-Value Personnel) |
|---|---|---|---|
| **Hypergrowth** | **Shipyard** (own single-column screen, `shipyardView` in render.ts — NOT the two-column `builderView`; see `HYPERGROWTH-SHIPYARD.md`) | Ship types with a quantity (stepper on the right of the name line). Cap is a **two-button choice: ¢300bn / No Limit** (`sy-cap-limited` / `sy-cap-nolimit`; No Limit = `unlimitedShipyards`, suppresses the ship list, no ∞, no explainer). Also in the New Fleet modal (`nf-nolimit`). | **Choose up to 3** (7 faction + 5 generic in ONE flat list, no headings/cards, check on the right), no assignment here. |

**Hypergrowth Play Mode** is its own layout (`isShipyard` branch in `playView`): faction ability with the Commands directly under it (left col); the right col is the live **Shipyard requisition tracker** (`playShipyardTracker`) — every class always visible with Deploy / Jumped out / Jump in and a yard·play·reserve tally (`play.req[classId]={play,reserve}`, actions `play-deploy`/`play-jumpout`/`play-jumpin`), NOT the damage-box tracker. Phase content is verbatim and mode-aware (`phasesFor(mode)`): Command Phase = checkboxes A/B; shipyard Jump Phase = a no-checkbox reference block (Open Jump Point / Requisition / Jump In); scoring reminders render in the **End Phase only**. Arc glyphs (`arc-primary` wedge + dome outline, `arc-aux` filled dome) and the left-aligned weapons table (`.wt-row` name col = `1fr`) are global, all modes.
| **Armageddon** | **Fleet List** | Discrete units. | **Chosen + assigned pre-play** (enforced). |
| **Age of Unity** | **Fleet List** | Discrete units. | **Split.** Choose at build (offered, not enforced — `HVP_COUNT` dropped); assign in Play Mode once missions are rolled (p.92 step 5). Print shows the chosen ones, or the full menu if none chosen yet, plus a write-in slot per ship. |

### ✅ Phases A–E — all DONE
- **A** (v0.8.26) Two-column builder: sticky roster LEFT, catalogue + HVP pool RIGHT. `builderView()`, CSS `.mf-body`/`.mf-manifest`/`.mf-yard`.
- **B** (v0.8.35/.36) `SavedList.unlimitedShipyards` + toggle in the credits popover; drops `OVER_BUDGET`, shows `∞`. Hypergrowth credits = 300 or Unlimited only, in both the New Fleet modal and the builder.
- **C** (v0.8.30) Per-mode HVP: `HVP_COUNT` dropped for hypergrowth / age-of-unity / management-training; HVP section hidden for hypergrowth, "assigned after missions" note for Age of Unity.
- **D** (v0.8.32/.35) Print: an **Actions & Commands** reference (see `src/data/commands.ts`), ink-friendly headings, much tighter spacing; Age of Unity gets an "HVP carried" write-in column + an "Available High-Value Personnel" section. Shipyard modes keep their requisition tick-boxes.
- **E** (v0.8.34/.37/.38) Emblem picker rebuilt as **one modal** (`emblemModal()`) with Library / Upload / Colour tabs, used by the builder, foundry, and solo alike. Library has folder chips + search; sigil labels are cleaned of filename boilerplate. Ship image is a click-to-upload tile.

---

### Print setup (v0.8.45/.46) — how the preview works
The sheet is laid out at the **true printable width** for the chosen paper (`PAPER` in `state.ts`: Letter 710×950, A4 688×1017 CSS px inside the 14mm `@page` margin) at *every* screen size. On a narrow screen it scrolls inside `.sheet-viewport` rather than reflowing — if it reflowed, it would break in different places than the printer and the page count would lie. `paginatePrintPreview()` (`main.ts`) walks the sheet's **leaf** breakable units (table rows inside tables; recurses into any block taller than a page) and draws absolutely-positioned `.page-guide` lines plus a live page count. It runs after layout settles (rAF) and again on `document.fonts.ready`. There is deliberately **one renderer** — preview and paper are the same DOM and stylesheet.
`PrintOpts` (format, trackers, rules, paper, inkSaver, excluded[]) persists to `abs2.print.v1`. Ink saver (default on) drops every coloured fill so output is identical whether or not the browser prints background graphics.
**Not built:** a real "Download PDF" — that needs a PDF lib, and a button that just calls `window.print()` would be a lie. Chrome's "Save as PDF" destination covers it for now.

## Recently done (verify on live, don't redo)
- **Starter lists ship as pre-saved fleets** (v0.17.0). `src/data/premade-lists.ts` was data nothing read. It was briefly offered in the New Fleet modal (a card with a "load this list" button); that was removed at the user's direction - a ready-made fleet does not need pitching, it needs to be already there. `PREMADE_SEEDS` in `web/seed-lists.ts` maps the three into `SEED_LISTS`, so all eight starter fleets seed onto the Fleets page. **Every seeded fleet costs exactly 300**, enforced by `test/premade-lists.test.ts`. Aegis and Gen Omega had to change shape to get there (Aegis prices its four heavies odd and everything else even, so an odd NUMBER of heavies can never total 300) - the reason is in a comment above each list.
- **Rules audit, movement procedure, capital-ship names** (v0.23.0).
  - **AUDIT FINDING.** Drag to Select was quoting p.34 (three numbered steps + a trailing paragraph). p.31 prints the same list as FIVE steps and the fifth is *"Once all in-play units have activated, the Tactical Phase ends."* — the only statement anywhere of when the phase ends, and the one line we had dropped. Now uses p.31's superset. Also: the book says **Unit Coherence**, not Cohesion; we had it wrong in two places.
  - Movement Step is a lettered `steps()` procedure (A pivot / B move ahead / C check coherence) with Inertial Strain as a sub-bullet under the pivot. Power to Engines is a heading + the real Command card (`cmdCard()`, same styling as the Command Phase page) + a picture that now **turns** at the halfway mark, because a second pivot is half of what the token buys.
  - Jump Hop and Jump Out moved from the Jump Phase to Movement: both are things a unit does with its own activation.
  - The three Jump Phase turns are three separated cards, not one segmented strip. Opening a Jump Point draws the 9" gravity well and a point out past a sector boundary ("any table"). Jumping In shows a unit in coherence — **the bubble is 52px for 6", so 1" = 8.67px**; the widest pair was landing at exactly 52 (legal only by rounding) and is 49 now.
  - `pass` rebuilt: six hops round four seats, P1 dimming after its own turn and being skipped thereafter.
  - **`src/ship-names.ts`** (new): 108 multi-word capital-ship names transcribed from `unsc-ships-halopedia.md`. Every **Mass 3** unit is auto-named on add (`add-unit` in actions.ts), seeded on the unit's own id so it never re-rolls on a repaint, and de-duplicated within the fleet. Used for every faction, not just the UNSC example. Covered by `test/ship-names.test.ts`.
- **Command Phase rebuilt; diagrams gated on visibility** (v0.22.0).
  - Command Phase order is now gain -> what a token is -> the six Commands -> Initiative, and the one diagram that did two jobs is three: `cmd-gain`, `cmd-spend`, `initiative`. Core Commands are numbered in the BOOK's order. Red Alert was missing its last sentence. Cut on request: the Training Fleet token count and the "equivalent to rolling against Silhouette 3" mnemonic.
  - **Diagrams only animate while on screen.** `observeDiagrams()` in main.ts. Two traps: (1) `is-onscreen` lives in the class attribute and render() emits a bare `class="learn-dg"`, so morphing strips it and IO has no change to report - it **disconnects and re-observes on every paint**; (2) the CSS pause rule is gated on `.dg-gated`, which only the observer's constructor sets, so no-IO or any script failure degrades to everything playing rather than to frozen pictures.
  - Getting Prepared: "Multiple tables?" sub-heading, an italic "No really... multiple tables?" fold over the book's sidebar, and Tokens / Other minis & terrain as folds under "when you're ready for bigger games" at the end of the page. Jump Phase lost its vignette and the "Your fleet starts off-board" heading.
  - The home rows lost their clipped bottom-right corner. Five stacked rectangles that were not rectangles read as a mistake on a page of hard Swiss edges; the hover sweep is the flourish.
- **Usability pass: touch targets and contrast** (v0.21.0). Audited every interactive element and every text/border colour pair on twelve views at 390x844 and 1280x900. Now zero failures at both widths.
  - **Two floors, not one.** WCAG 2.5.8 (AA) is **24x24 for any pointer**; the app's own 44px rule is the touch standard on top of it and stays gated on `max-width: 760px, (pointer: coarse)`. `.cg-build` was 21px, which failed even the mouse floor.
  - **`--hair-soft` (1.43:1) was being used as a control edge** on `.ghost-btn` and `.faction-plaque`. It is the quiet-divider token; `--field-edge` (3.52:1) exists precisely because a control with no fill and no shadow has nothing but its border to identify it (1.4.11). Destructive controls were worse: `--red-wash` at 1.14:1, an invisible warning, now `--red` (3.6:1).
  - **`--ink-off` (3.52:1) is not a text colour.** It clears the 3:1 a boundary or a disabled glyph needs and fails the 4.5:1 live small text needs. It was on `.ltp-dot` and the damage-table caption; both are `--ink-3` (5.96:1) now.
  - **White on `--red` is 3.6:1** — fine as a keyline, fails as small text. The A/B/C chips and the Tactical step numbers use `--red-deep` (5.07:1).
  - **The ordering trap bit twice.** `.ltp-subtab` was listed in the coarse-pointer block ~1000 lines above its own rule and lost; `.cg-build`'s 24px floor then beat the 44px touch rule for the same reason. style.css is unlayered — assert a size override *next to* the component, not in a distant block.
  - **Known false positives** for anyone re-running this: `.wm-suns` is `color: transparent` with a 2.5px white `-webkit-text-stroke`, and `.ltp-pick-radio` is a 1x1 visually-hidden input whose `<label>` is the real target.
- **Type scale, Tactical sub-pages, and a link audit** (v0.20.0).
  - **The pages were set too small.** Body prose 15px -> 17px, the book's italic-serif passages 16 -> 18, section titles 30 -> 34, and everything else up to match; the measure widened 780 -> 840 to keep the line length around 66 characters. Narrow screens get the same bump one notch down.
  - **The Tactical Phase is five sub-pages** at `#/rules/tactical/<step>` (`TACTICAL_SUBS` / `TACTICAL_PAGES` in `learn.ts`, `TACTICAL_SUB_IDS` in `state.ts`), with a numbered sub-rail under the heading. It was longer than the other three phases put together. Previous/Next walks `READING_ORDER`, which expands Tactical into its five steps so paging through does not skip four fifths of the longest phase.
  - **Drag to Select is a pulse now, not a marquee.** No corner brackets, no dashed range ring, no selection box - press the lead, a wave goes out to exactly 6", and what it touches turns blue. Hull size follows Mass on a steep ramp. **Mass tops out at 3**: the diagram had a Mass 4 lead summing to exactly 10, which is not a fleet that can exist. It is 3+2+1+1 = 7 plus three Mass 0 squadrons, which also shows that Mass 0 costs nothing against the cap.
  - **Both jump diagrams were broken.** The landed jump point had a `transform` attribute AND a CSS animation touching transform, so it drew at the SVG origin (the trap documented at the top of `diagrams.ts`). Jump In's animations were one-shot, and both diagrams now live inside a picker panel that starts `display: none` - a one-shot on a hidden element either never runs or holds a dead end-frame. Both loop on the same 3400ms cycle now.
  - **Every miniature-source link was followed.** Star Wars: Armada pointed at BGG id 181834, which is an RPG supplement called "Universal Adventures Encounter Pack"; it is 163745. Everything else resolves and sells what it says. DriveThruRPG 403s to bots and is fine in a browser.
  - **The `book` icon was a plain empty square** at the sizes it is used at - home page, onboarding card, footer, buy CTA. Replaced with Tabler's open book. The footer's Discord link used the `commander` org-chart glyph; it uses the real Discord mark from Simple Icons now. **Brand marks get traced from the source set, never approximated.**
- **All six pages on one rail** (v0.19.0). The routes stay split (`#/learn`, `#/rules`) but the tab strip lists all six, each link carrying its own root (`ALL_PAGES` in `learn.ts`), with a hairline on the Command tab marking the boundary. Showing only two or only four made the other half of the walkthrough invisible from where you were standing.
  - **The Jump Phase's three turn options are an A/B/C picker** (`turnPicker()`): radios + CSS sibling selectors, no JS and nothing in the store. Each option owns its rules and its own diagram — `jump-point`, `jump-in` and a new `pass` (turn order going round a ring, one seat going quiet and being skipped). Deliberately not store state: routing it through the store would re-render the page and restart the diagram on every press.
  - **The drag-select diagram lost its brackets and gained scale.** Four sets of corner ticks plus a marquee was five squares of lines on top of a circle; selection is carried by the hulls turning blue instead. `SHIP()` takes a scale now, applied on the OUTER group (never the inner one, which the animation classes own), and hull size follows Mass so the 4+3+2+1 sum reads off the shapes.
  - Also cut on request: the two opening CMD-token paragraphs on Command, the whole Hypergrowth Shipyard section on Jump, and the End Phase vignette. The Combat Simulator deployment map moved from Getting Prepared to "Your first game", which is the scenario it actually shows.
- **Split in two, and every word on it is now somebody else's** (v0.18.0). `#/learn` is the front of the game (Pick an era, Getting prepared); `#/rules` is a round phase by phase (Command, Jump, Tactical, End). Both render through `learnView()` via `GROUPS` in `web/learn.ts` — same header, tab strip and pager, different tab list. A phase name under `#/learn` forwards to `#/rules` (`parseRoute`), because those links were live for a fortnight. The pager crosses between the groups at each end, so the read-in-order path survives the split.
  - **The "why this is cool" callouts are the BOOK talking now.** They were our voice, in a bordered box under a "WHY THIS IS COOL" label, explaining to the reader why the rule they had just read was clever — nineteen times. They are the rulebook's own designer notes, sidebars and chapter vignettes (p.29 sectors, p.32 jumping, p.41 combat, p.49 commands, p.31 end phase, p.73 eras), set as plain italic serif paragraphs with no box and no label. `cool()` in `learn.ts` is one div now. **Do not write a new one.** If a section needs an introduction, find one in the book.
  - Also cut in the same pass: the four-phase round strip, "Three eras, three games", the "In this app:" line, "Not sure? Armageddon is the simplest way in", every section lede, and roughly a dozen paragraphs that restated a rule quoted directly beneath them or a diagram directly above them.
  - Jump Phase has TWO diagrams now (`jump-point`, `jump-in` in `web/diagrams.ts`) because opening a point and jumping a unit in are alternative turns, not one sequence. Jump Strain is gone from the page.
  - The era animations run at `ERA_SLOW` (2.8x) — `decodeTitle`/`wipeTitle`/`slamTitle` take a multiplier and the faction picker still passes 1. Armageddon gets fire (`.is-burning` on the `<details>`, two gradient layers on `.ltp-era-body`'s pseudo-elements — read the comment there before touching the stacking or the gradient sizing, both got this wrong once). Age of Unity gets a perspective crawl on top of the wipe.
  - Miniature sources: the "Ask somebody" group is gone and the Planet Smasher Discord moved to the site `footer()`.
- **Learn to Play was six pages, not one scrolling page** (v0.17.0). `learnView` renders only the tab in the URL; the scroll-spy, the reading-progress bar and the "Section N of 6" kicker are gone from `web/learn.ts`, `web/main.ts` and `web/style.css`, and a `.ltp-pager` at the foot of each page keeps the read-in-order path. `quote()` no longer takes or prints a page citation (the `// p.12, verbatim.` comment above each call is now the only record, which is where it was always written). The ~40 miniature sources are behind one closed `<details>`. Several ledes and "why this is cool" callouts were cut as padding, along with the "In this app:" line on each era card.
- **Round emblems is no longer a setting** (v0.17.0). The disc crop is unconditional; `Settings.discCrop`, the `toggle-disc-crop` action, the `no-disc-crop` root class and its CSS are all gone. A stored value from an older build is ignored, no migration needed.
- **`src/data/index.ts` registered only four factions** (v0.16.0). It had listed the Armageddon four since they were the only ones transcribed, and never grew — the app never noticed because `web/catalog.ts` finds faction files with `import.meta.glob` and passes the validator its own catalog. But `defaultCatalog`, which every non-browser caller gets, could not resolve an Age of Unity or Hypergrowth faction at all: validating one returned a flat "Unknown faction". All twelve are registered now, and `test/data.test.ts` walks all of them (which is how the `chief-engineer` collision above turned up). Adding a faction file means adding it to that list too.
- **`test/premade-lists.test.ts`** (v0.16.0, new): every starter list is checked against the real catalogue — faction exists and matches its era, every `shipClassId` is on that roster, three distinct available HVP, and a full `validateFleet` pass with no errors and no warnings. These are hand-copied id strings, so a ship or role rename would otherwise break them silently.
- **Rich text for faction backstory/notes** (`web/richtext.ts`, new). Real Markdown, not preserved line breaks: `#`/`##`/`###` headings, `**bold**`, `*italic*`/`_italic_`, `` `code` ``, `-`/`1.` lists, `[text](https://…)` links. Deliberately Markdown-in-a-`<textarea>` + toolbar (`markdownEditor()`) rather than a contenteditable WYSIWYG — this app full-re-renders through morphdom on every state change, which fights any editor that owns its own DOM, and a `<textarea>` fits the existing uncontrolled-input pattern (commits on blur/change) that every other text field already uses. `renderMarkdown()` escapes the entire input FIRST, before any markdown→HTML substitution, so only tags the function itself emits can appear in output; links are restricted to `https?://`. The Foundry editor's backstory field uses `markdownEditor()`; the rendered result now surfaces in `factionRuleBlock()` (`web/render.ts`) under the faction's rule text wherever that block is used (builder, picker) — it used to be trapped inside the editor where nobody but the person editing it ever saw it. Live preview is wired by `wireMarkdownPreviews()` in `web/main.ts`'s `paint()`.
- **"Load example factions" is now opt-in** (`web/example-factions.ts`, new; offer logic in `render.ts`'s `examplesCallout()`/`examplesOnOffer()`, actions in `actions.ts`). The Covenant/UNSC/Posthuman Republic worked examples surface as a "Want to see some examples?" callout on the Foundry screen starting at the 5th visit (`EXAMPLES_FROM_VISIT`), and only write to `state.customFactions` if the user clicks yes — never auto-seeded. Replaces the old `cf-covenant` seed, which landed in every browser uninvited and had to be cleaned back out of everyone's storage.
- **Solo starter outfits** (`src/data/starter-outfits.ts`, `web/solo.ts`'s `newOutfitModal()`): two ready-made Junkspace crews, Greylancer and Valcua, offered alongside "Start blank" in the new-outfit modal. All ship/pilot names are pulled verbatim from the user's own Steel Rift "🎌" callsign pool (`Steel-Rift-Jet/src/callsigns.js`) — house rule now: never invent flavor names for this project, always source from that pool or ask for another one.
- **Learn to Play, rebuilt** (v0.15.0, `web/learn.ts`, new): one long scrollable page at `#/learn` with a sticky progress header and six named tabs (`eras`, `prepare`, `command`, `jump`, `tactical`, `end`). Tabs sit in the header above 900px and in a bar fixed to the bottom of the viewport below it, in the thumb zone; both strips are rendered and one is hidden by media query. Scroll-spy, progress bar and era-title animations are wired in `paint()` (`syncLearnScroll`, `syncLearnTarget`, `animateOpenEraTitles`) plus a captured `toggle` listener for the era accordions. The era headings reuse the faction picker's animations by carrying `.nfd-title` + `data-era`/`data-title`; `animateFactionTitle()` excludes them with `:not(.ltp-era-title)`. Every rules quote is verbatim with a page cite; the "why this is cool" callouts are ours and are the only italic serif on the page. Reached from the home row AND the first-run onboarding card, which used to offer the two Basic Training scenarios and now points here.
- **The old walkthrough is archived, not deleted** (v0.15.0): `learnClassicView()` in `render.ts`, routed at `#/learn-classic[/:step[/:anchor]]`. Nothing in the app links to it. `parseRoute` sends a NUMERIC first segment under `learn` there too (`#/learn/3/tactical`), so every URL anyone shared before the rewrite still resolves; a NAMED first segment is the new page. Prose that turns out to be worth salvaging can be lifted from there.
- **`massGlyphs()`** (v0.15.0, `web/format.ts`): the Mass glyph swap with no escaping, for rules prose written as an HTML literal in source (which is all of `learn.ts`). `ruleText()` is still the rule for anything from data or from a person.
- **Emblem colour system** (v0.8.37): `emblemColor` (SVG tint, via CSS mask) + `emblemBg` (coloured ground, works on any sigil) on `SavedList` / `Faction` / `SavedOutfit`; rendered by `emblemView()` in `web/render.ts`; generic `emblem-set-color` / `emblem-set-bg` actions patch whichever target the modal edits.
- **Share links are compressed** (v0.8.33): `deflate-raw` via Compression Streams behind a `#z=` prefix, plain `#s=` fallback; both decode. See `web/share.ts`.
- **Solo** (v0.8.36/.37): outfit roster shows full stats + PRI/AUX weapons; the Stock Ship Classes catalogue reuses the fleet builder's row (shared `weaponsTable()`, exported from `render.ts`); landing page shows debt-progress cards.
- Mass Ⓜ glyph → inline SVG circled-M, centred (v0.8.25, `ruleText()` in `web/format.ts`).
- Mass Ⓜ glyph → inline SVG circled-M, centred (v0.8.25, `ruleText()` in `web/format.ts`).
- Age of Unity title = Star Wars recede; Hypergrowth = decode/scramble; Armageddon = vertical thump + red underline drawn at ~190ms (v0.8.22/.25). Desktop ≥992px, non-reduced-motion only.
- New Fleet era chooser = 3 distinct buttons (v0.8.25). Command tokens shown as N triangle marks like the dice; AEGIS/Golem `>`-slogans render as a mono terminal line with a blinking cursor (v0.8.24). Initiative dice + command marks share the value's line and pop in.
- Options dialog (top-bar gear) = data export/import/clear + about links (v0.8.21). Version/changelog link removed from the footer (it's in Options now).
- Compendium: filters + "Sort by faction" + "Show custom ships" (off by default) all in one bar (v0.8.21/.24). Solo pilot-class = 3-way icon picker (gunner/hauler/junker, `web/pilots/*.png`) (v0.8.23). Adding a ship animates just the new roster row (v0.8.23).

---

## Architecture

- **Framework-free.** One `Store<AppState>` (`web/state.ts`, `web/store.ts`). Every state change re-renders the WHOLE app as an HTML string (`render(state)` in `web/render.ts`) and replaces `#app.innerHTML` (`paint()` in `web/main.ts`). No DOM diffing. `setState` is synchronous — the DOM is repainted by the time it returns.
- **Events:** `data-action="..."` + delegated `click`/`change`/`input` in `web/actions.ts`. Add a `case` in `handleClick` (clicks) or `handleChange` (selects/inputs/file). `actions.ts` is the only place that mutates state.
- **Routing (hash):** `#/fleets`, `#/list/:id` (builder), `#/ships` (compendium), `#/solo`, `#/foundry` (Custom Rules), `#/play/:id`, `#/print/:id`, `#/learn[/:tab]` (eras, prepare), `#/rules[/:tab[/:sub]]` (command, jump, tactical + five steps, end), `#/learn-classic[/:step[/:anchor]]` (the archived walkthrough, unlinked). Share links are NOT routes: `#s=`/`#z=` payloads are intercepted at boot by `tryImportShare()`.
- **Post-render imperative hooks** in `paint()` (`web/main.ts`), run after `innerHTML` is replaced: `enhanceNav()` (nav pill placed WITHOUT transition so it doesn't re-animate each click), `animateFactionTitle()` (era-keyed title entrance, keyed to fire only on a real title change), `animateNewRosterRows()` (entrance on a newly-added roster row — rows carry `data-roster-key="<unitId>"`, diffed vs the previous set), `positionTour()`. Reusable pattern: track a prev-key set in a module var, diff each render, act only on new elements, reset the var when the target is absent.
- **Persistence:** `web/storage.ts`, one `abs2.*.v1` localStorage key per concern (`abs2.lists.v1`, `abs2.customFactions.v1`, `abs2.outfits.v1`, `abs2.onboarding.v1`). `exportAllData`/`importAllData`/`clearAllData` scan the `abs2.` prefix.
- **Data model:** `SavedList` (`web/storage.ts`) = `{ id, mode: GameMode, freePlay, emblem/emblemImage/emblemLib/emblemColor, fleet: Fleet, play?, createdAt, updatedAt }`. `Fleet` (`src/types.ts`) = `{ name?, factionId, creditsLimit, units: FleetUnit[], hvp: FleetHvp[], notes? }`. `FleetUnit` = `{ id, shipClassId, count, name?, species?, ... }`. `GameMode` = `combat-simulator | management-training | armageddon | age-of-unity | hypergrowth | junkspace`. Factions/ships in `src/data/`; rules + validation engine in `src/` (has `node --test` tests). Custom factions live in `state.customFactions`. `HIDDEN_FACTION_IDS`/`HIDDEN_FACTION_NAMES` in `web/catalog.ts` hide seed/template factions from `allFactions()` while keeping them resolvable for saved fleets via `findFaction`.
- **Solo/Junkspace:** `web/solo.ts` (own outfit/pilot/campaign model).

## Design system + hard rules
- Light NASA/Swiss theme. Tokens: `--blue #0b3d91`, `--red #fc3d21`, `--ink #0a0e14`, white ground. Fonts: Archivo (`--display`), Libre Franklin (`--sans`), Source Serif 4 (`--serif`). `* { box-sizing: border-box }`. Flat, square corners, no gradients/shadows.
- **Non-negotiable UI rules (user's global CLAUDE.md):** (1) never truncate/hide gameplay text — no ellipsis / `overflow:hidden`+nowrap / `display:none` on names, stats, rules; let containers grow. (2) No layout shift from any state change — popovers/panels are `position:absolute/fixed` off a relative trigger; never an inline-expanding `<details>` that shoves content. (3) Every button/link pairs an icon WITH a visible text label.
- **NOTHING MAY MAKE THE UI JUMP.** No click, no menu, no navigation, no state change may move anything the user was not aiming at. This is broader than rule (2) above and it is the one users notice fastest. The three ways it has actually happened here, all of which are now fixed and none of which look like a layout bug in the source:
  - **The scrollbar.** A short page has none and a long page has one, so the viewport is ~13px narrower on one than the other and every centred element slides sideways when you navigate. `html { scrollbar-gutter: stable }` reserves it on every page. Never remove that line.
  - **Full re-render.** Replacing `innerHTML` restarts every transition and resets scroll positions. `web/morph.ts` updates in place instead; keyed elements survive, and so do their scroll offsets.
  - **A control whose own row changes height when you press it.** The catalogue row briefly swapped its weapons table for a shorter "in your fleet" line once you owned one, so the row collapsed ~50px under the cursor on ADD. A row must be the same height before and after its button is pressed.
  - **Content growing above what you are looking at.** Adding a ship grows the roster by a whole row; where the roster stacks above the catalogue that pushed the list down ~240px mid-click. `holdAnchor()` in `web/main.ts` fixes this generally: `pointerdown` records the pressed `[data-action]` element's viewport top, and after the repaint the page is scrolled by however far that element moved. The browser's native scroll anchoring only covers growth ABOVE the viewport and gives up at scrollTop 0; this holds at any position. It deliberately does nothing across a route change.
  - **Content of varying height inside a fixed frame** (a 591px diagram inside a phase checklist) shoves everything below it on each switch. Give the frame a stable height or move the variable thing out of the flow.
  When you change layout, check it by navigating between a SHORT view and a TALL one and measuring that a fixed reference (`main`'s `left`) does not move. Measuring one page tells you nothing.
- **Icon-only is allowed in exactly one place:** a compact action strip on a card, of at most three actions that are universally understood (duplicate / share / delete), each carrying a `title` AND an `aria-label`. That is `.card-act` on the fleet cards. Everywhere else rule (3) holds: icon AND visible text. The same action uses the same glyph wherever it appears, menu or strip.
- Copy/writing style: no em-dashes; plain, varied prose (changelog entries and UI copy).
- **RULES TEXT IS VERBATIM. ALWAYS.** Any string the app presents as a rule — faction rules, HVP rules, Commands, Actions, phase and activation-step text, scenario and solo rules, the Learn to Play and Rules pages — is transcribed word for word from the book or the Quick Reference and displayed in full. Never summarise it, never paraphrase it, never tighten it, never write it from memory, never shorten it to fit a layout, and never drop a qualifying clause because it is long. If it does not fit, change the layout. If you cannot find the source text, extract it from `web/public/ABS-2E-Quick-Reference.pdf` or ask — do not reconstruct it. This has been got wrong repeatedly (see the changelog entries about the Passive Attacks Step and the Blockading tie-breaks), and each time it shipped a rule that was simply false.
- **Mass is Ⓜ** (U+24C2, the circled M) — the game's own symbol, in the data and in the UI. Rules prose goes through `ruleText()` (`web/format.ts`), NOT `escapeHtml()`, which is what draws the circled M; the only exceptions are `<textarea>` values, where the raw character must survive the round-trip, and `title=` attributes, which cannot hold markup. The `stat-mass` icon in `web/icons.ts` is the same mark; if one changes, change the other. It is not a hexagon.

## Build / test / deploy

```bash
# from D:/wargaming/Web Apps/ABS-V2  (Git Bash)
npx tsc --noEmit -p web/tsconfig.json   # typecheck — MUST pass
node --test                              # unit tests — MUST pass
npm run build                            # vite build -> dist/
```

**Deploy — GitHub Pages serves the `gh-pages` branch (legacy Pages, NOT Actions). Pushing `main` does NOT deploy.**
1. Bump `package.json` "version". That is the only place a version lives now — the Options dialog reads `pkg.version` directly (`optionsModal` in `web/render.ts`). There is no longer a `web/changelog.ts`; the in-app changelog was dropped, so do not go looking for a `CHANGELOG` array to add an entry to.
2. On `main`: stage changed files **explicitly** (not `-A`), commit, `git push origin main`.
3. Sync `dist/` into the gh-pages worktree and push:
```bash
MSYS_NO_PATHCONV=1 robocopy "D:\wargaming\Web Apps\ABS-V2\dist" \
  "C:\Users\wongj\AppData\Local\Temp\abs-gh-pages-worktree" /MIR /XF .git .nojekyll /NFL /NDL /NJH /NJS
# robocopy exit 0-7 = success (3 = files copied). MSYS_NO_PATHCONV=1 stops Git Bash mangling the /flags.
cd /c/Users/wongj/AppData/Local/Temp/abs-gh-pages-worktree
git add -A && git commit -m "Deploy vX.Y.Z" && git push origin gh-pages   # -A is fine here (deploy output only)
```
- gh-pages worktree: `C:/Users/wongj/AppData/Local/Temp/abs-gh-pages-worktree` (its `.git` is a pointer → `.../.git/worktrees/abs-gh-pages-worktree`; recreate if missing). Keep `.nojekyll` and `ABS-2E-Quick-Reference.pdf` in the worktree (hence `/XF .git .nojekyll`; Vite copies the PDF from `web/public/`).
- Git conventions: explicit staging on `main`, never force-push, never `--no-verify`, always new commits (never amend). Co-author line: `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
- Live: https://type37.github.io/a-billion-suns-shipyard . Repo: Type37/a-billion-suns-shipyard.
- Leave untracked: `HANDOFF.md` (this file), `abs-1e-vs-2e-comparison.docx`, `abs-play-mechanics-delta.docx`.

## Verification gotchas (IMPORTANT)
- The **in-app preview Browser freezes its animation timeline** — WAAPI (`element.animate`), `requestAnimationFrame`, AND CSS transitions do NOT advance there, and screenshots hang. You CAN verify structure/layout/computed-styles/which element got an animation via `javascript_tool` DOM measurement; you CANNOT watch motion play. So any animation that relies on the timeline to reach its visible end-state must have a correct **resting** state in the server-rendered HTML (e.g. decode spans seed the real character; the Armageddon underline is shown at rest and only held collapsed via an `is-landing` class during the slam). Verify motion structurally, eyeball it on the live site.
- Viewport sometimes reports 0×0 — measure text with a fixed-width OFF-screen element rather than trusting `getBoundingClientRect` on laid-out flex/grid children.
- `preview_start { name: "abs-builder-dev" }` runs the vite dev server (`.claude/launch.json`). Reload after edits if HMR looks stale.

## Key files
- `web/render.ts` — all views (large): `builderView`, `newFleetModal`, `factionDetailPane`, `foundryEditView`/`foundryListView`, `printView`, `playView`, `learnView`, `shipsView`, `soloListView`(via solo.ts), `optionsModal`, `emblemModal`, `footer`, `topbar`, `catalogShipRow`, `weaponsTable` (exported — solo reuses it), `emblemView`, `weaponEditor`.
- `web/actions.ts` — all `data-action` handlers.
- `web/main.ts` — `paint()` + post-render animation/nav hooks.
- `web/state.ts` / `web/store.ts` / `web/storage.ts` — state, store, persistence, data export/import.
- `web/style.css` — all CSS (single file).
- `web/icons.ts` — inline SVG icons + `diceRow`/`commandRow`/`commandToken`/`massGlyph`/`initiativeDice`.
- `web/format.ts` — `ruleText` (renders the Ⓜ SVG), escaping, dates.
- `web/solo.ts` — Junkspace solo mode; `web/catalog.ts` — `allFactions`/`HIDDEN_FACTION_IDS`; `web/faction-lore.ts` — per-faction slogans (`tagline` only); `web/changelog.ts` — in-app changelog; `web/emblems.ts` — emblem/icon library (`import.meta.glob` over `web/emblems/**`).
- `src/` — rules data + validation engine + tests. `src/data/commands.ts` holds the 7 core Commands + 6 Actions, transcribed verbatim from `web/public/ABS-2E-Quick-Reference.pdf` (that PDF is the authoritative source; extract it with `python -c "import pypdf; ..."` if you need the text again). Faction rules, HVP rules, and scenario rules are where EXTRA commands come from, so the print sheet prints those in full alongside the core list.
